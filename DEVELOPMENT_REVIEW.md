# B3 專案接手紀錄

檢查日期：2026-09-15。下載來源：rilakkumagavin/b3-ai-workshop，基準提交：29ff118。

## 已讀取的開發過程

提交依序加入任務站、Google 表單與 Apps Script 連線設定、正式試算表 API、冷啟動等待、Google 重新導向提交收據、風險策略保留與規範排版，最後加入靜態示範頁與完整專案備份。

已閱讀根目錄 README、project-backup 的 README／BACKUP_README、正式部署紀錄、互動與 Dashboard 檢查報告、資料庫重用與個資部署文件，以及現行前端設定、管理面板、API 與 Apps Script 路由。

根目錄 README 描述早期靜態示範，不能單獨當作現況。備份中的部分文件記載尚未部署，較新的 production-deployment.md 則記載正式驗證完成；這些是歷史紀錄，本次沒有重新執行線上提交驗證。

## 需求與現況

| 需求 | 現況 | 待完成 |
| --- | --- | --- |
| 管理員模式 | admin.html 公開；admin.js 僅寫入 localStorage | 後端驗證管理員，未授權不得讀寫設定 |
| 多人共用資料 | 已有 Apps Script 寫入試算表，寫入使用 ScriptLock | 驗證兩個獨立瀏覽器共用同場資料；加入受控定時更新 |
| 即時成果牆 | Dashboard 開頁與手動按鈕讀取資料 | 定期更新、避免重疊請求、錯誤保留畫面 |
| 貼試算表網址 | 現有欄位接受 Google 表單與 Apps Script URL | 管理 API 解析試算表網址、驗證部署者存取權及欄位，再儲存後端設定 |
| 學員看不到設定 | SPREADSHEET_ID 由 Script Properties 讀取；公開備份另有試算表網址 | 清理公開文件，敏感設定只由已驗證管理員取得；公開 Git 歷史也需納入處理 |
| 全班設定同步 | localStorage 覆寫只影響當前瀏覽器 | 所有學員使用固定 API；資料庫選擇在後端統一處理 |

## 實作方向

保留既有教材與十三張工作表，以固定 Apps Script API 連接受限制的試算表。管理員驗證必須在伺服器執行；隱藏導覽列或前端密碼比較無法保護設定。公開前端仍必須知道 API 網址，因此 API 網址不能視為秘密或管理員憑證。

管理員登入後貼入試算表網址，後端先檢查權限與 schema，成功才切換；切換與提交使用同一把鎖，避免寫入錯場。學員 API 僅回傳必要的公開場次資訊與投影資料。若需要同時多場，須另設場次到資料庫的後端映射，不能用單一全域設定互相覆蓋。

部署驗收包括：未登入讀寫管理設定遭拒、學員回應不含私有設定、兩裝置提交可見、同時提交不遺失、切換失敗保留原設定、斷線與重試不重複寫入。既有收據機制不能直接等同防止重複提交。

## 本次完成範圍

已下載完整 Git 專案與歷史，完成程式及開發文件盤點。尚未修改功能、發布 GitHub Pages、改動 Google 試算表或部署 Apps Script。
