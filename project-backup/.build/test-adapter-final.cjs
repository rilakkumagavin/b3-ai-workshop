const vm=require('vm'),fs=require('fs'),assert=require('assert');const c=vm.createContext({});vm.runInContext(fs.readFileSync('public/assets/js/sheet-contract.js','utf8')+'\n'+fs.readFileSync('public/assets/js/sheet-adapter.js','utf8'),c);const r=c.b3ToSheet('submitRiskCase',{riskType:'FACT',scenario:'測試案例',judgment:'SAFE',strategy:'學生先移除個資'},{groupId:'1'});assert.equal(r.strategy,'SAFE');assert.equal(r.student_reminder,'學生先移除個資');const t=c.b3ToSheet('submitTrafficLightRule',{greenRule:'我們可以：查資料。',yellowRule:'我們使用前要與老師確認並說明方式：改寫文字。',redRule:'我們不會：考試作弊。'},{groupId:'1'});assert.equal(t.class_rule,'在我們班，學生使用 AI 時，可以查資料，需要說明改寫文字，不能考試作弊。');console.log('PASS risk choice preserved and clean class sentence');
